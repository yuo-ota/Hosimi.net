DATABASE_USERNAME=
DATABASE_PASSWORD=
DATABASE_NAME=

RAILS_ENV=production
SECRET_KEY_BASE=

NODE_ENV=production

# CD がデプロイ時に上書きする、使用するイメージのタグ（commit SHA）。
# 手動運用や初回構築時は latest のままでよい。ロールバックは過去の SHA を指定する。
IMAGE_TAG=latest
